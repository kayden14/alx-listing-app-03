/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com"],
  },
};

module.exports = nextConfig;
// This configuration file for a Next.js application specifies that images can be loaded from the domain "images.unsplash.com".
// It allows the application to optimize and serve images from this domain efficiently. 